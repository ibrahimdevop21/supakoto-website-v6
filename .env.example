# SupaKoto V6 — public analytics IDs (inlined into the client bundle at build
# time; set the same four in Vercel → Project → Environment Variables for
# Production AND Preview). A missing ID disables that platform only.
# These are public identifiers — safe to record here. Values = V2's live IDs
# (Phase 18, 2026-08-19).
NEXT_PUBLIC_GA4_ID=G-ENPYD2K4R3
NEXT_PUBLIC_META_PIXEL_ID=1306471927697780
NEXT_PUBLIC_TIKTOK_PIXEL_ID=D32GSIRC77U649U91T50
# Base tag only (remarketing audiences) — no conversion actions in Phase 1.
NEXT_PUBLIC_GOOGLE_ADS_ID=AW-17767525580

# Canonical origin (already used by sitemap/metadata)
# NEXT_PUBLIC_SITE_URL=https://supakoto.com

# Forms → email (Phase 23). Key lives in Vercel env only.
RESEND_API_KEY=
FORMS_TO_EMAIL=info@supakoto.org
FORMS_FROM_EMAIL="SupaKoto Website <noreply@send.supakoto.org>"
