# Checkpoint — 2026-08-06 (end of session)

## What shipped today (14 commits)

1. **Redirects (was a deploy blocker)** — `docs/REDIRECTS.md` + 15
   permanent 308s in `next.config.ts`. V2 served EN-at-root/AR-under-/ar
   (inverted vs V6); collision paths documented as deliberate
   non-redirects. Curl-verified.
2. **V2 photography harvest** — 238 photos + 2 showreels into gitignored
   `assets/source/`; curated set live on hero/services/gallery/work-preview
   with fresh bilingual alt text. Reveal-threshold bug found and fixed.
3. **Booking unblocked** — confirm step logs intent client-side and opens
   a prefilled wa.me deeplink (always-Arabic body) to the regional line.
   bdm-flow edge function stays post-launch (notes in 05-pages.md).
4. **Phone ops decision applied** — Egypt line 01103402446 (call+WA),
   UAE call (050 line — retired 2026-08-16, see phase 15) / WA 971552054478; unowned V2 numbers purged;
   build guard `scripts/check-phone-literals.mjs` blocks phone literals
   outside `content/regions.ts` + `content/branches.ts`.
5. **TAKAI lineup** — full spec sheet in `content/takai.ts`; Signature=UAE
   (TAKAI SILVER = TAKAI 5 dual-named), Performance=Egypt, Premium Plus in
   both; matte flag on EG GOLD PLUS/STEEL PLUS; region-aware table on
   /services/ppf with warranty column.
6. **Warranty COMPLETE** — per-tier terms both regions (EG: 5/10/10/15/15/
   lifetime; UAE: 5/10×4/15/lifetime); comparison table fully filled;
   **lifetime qualifier resolved: LIFETIME OF THE VEHICLE, all tiers
   transfer on resale**. No warranty TODOs remain anywhere.
7. **Branches page real** — interactive Leaflet map (light Carto tiles,
   brand pins, popups), all six coords ops-confirmed on-site, directions
   deep-link to exact pins, six real branch photos, Dubai always visible
   under its own region heading, Al Quoz 4 correction.
8. **About page rebuilt as story** — documentary (umm7ZVZvBqo) opens the
   page full-width with muted autoplay + custom unmute button (YouTube
   IFrame API); story paragraph (rewritten to preview the journey) sticky
   beside the 2016→2026 timeline (11 milestones incl. Alexandria/Mansour
   and first EG Premium Plus, both 2026); stats after journey ("today in
   numbers", 25,000+ cars counter); hero + logo band removed.
9. **Reveal fail-safe** — after repeated invisible-section reports, all
   reveals force visible 2.5s post-mount; animation can never gate content.
10. **Home hero = V2 showreel** — full-bleed muted loop, desktop/mobile
    cuts via matchMedia, mp4 fallbacks transcoded for Safari/iOS, photo
    base layer for LCP/reduced-motion/failure.

## Start here tomorrow

1. **Stop the dev server, run `pnpm build`** — many changes landed with
   dev-server-only verification (build shares `.next`, so full builds were
   deferred). Lint/tsc/guard/parity are all green as of this checkpoint.
2. **Preview deploy** — Vercel CLI installed; needs `! vercel login`,
   then `vercel link` + `vercel` (preview only, NOT --prod). Runbook:
   `docs/progress/07-ship.md`. After deploy: disable Deployment
   Protection, verify in a private window.
3. Decide: founding year — timeline says **2016 per Ibrahim**; V2 said
   2018. Confirm 2016 stands.
4. Remaining gaps (none launch-blocking): before/after pairs; TikTok/
   YouTube/LinkedIn URLs (official YouTube channel exists — the
   documentary lives on it); branch working hours; franchise investment
   figures; heat-isolation + nano-ceramic spec TBC cells; RH-Zak
   web-embed licence confirmation.
5. Post-launch: bdm-flow public write path for real booking submissions.
