# Checkpoint — end of 2026-08-07 session (written just past midnight, 2026-08-08)

## TL;DR (2026-08-08)

The building heat isolation service is fully built and verified on branch
`feat/building-heat-isolation` (5 commits ahead of main, NOT merged, NOT
pushed). Main is live at supakoto-website-v6.vercel.app and still shows
the fabricated SK-BLD name until the branch deploys. Late in the session
the entire Arabic copy moved from Egyptian dialect to MSA (410/854 values,
CLAUDE.md rule updated). Everything is green; the session ended with an
8-check smoke list issued to Ibrahim — HE HAS NOT RUN IT YET. Nothing
merges until it passes.

## Phase ledger

- **Done (this session)**: full build verified green + preview verified
  live (redirects, locales, protection off) · building service P1–P4
  (spec, SK-BLD purge, routes, quote form, nav dropdown, 9 surfaces,
  assets doc) · Correction B (wa.me bodies follow active locale, booking
  too) · founding year confirmed 2016 · MSA rewrite site-wide · dev-server
  `.next` corruption fixed (kill dev before any `pnpm build`).
- **In progress**: smoke checklist v2 (8 checks, MSA criteria) — issued,
  awaiting Ibrahim's PASS/PARTIAL/FAIL results.
- **Next**: process smoke results → fix fails → merge + deploy on
  Ibrahim's word.

## Decisions this session (do not re-litigate)

1. Buildings = sixth service in existing line, `substrate` field; never in
   the car booking flow. Single SKU TK-7099-IR (10y, 70% VLT) — claim
   clarity WITH heat rejection, never "maximum"; no privacy claims.
2. SK-BLD is FABRICATED — purged; never reintroduce.
3. Both regions in the quote form; the form's region selection routes the
   wa.me line, overriding the RegionPicker. Quote marker is the first
   line, measurements second.
4. wa.me bodies follow the ACTIVE locale (/en English) — overrode the old
   always-Arabic rule; applies to BookingWizard too.
5. Nav gains خدماتنا dropdown — services were never nav-reachable before.
6. **Arabic register is MSA now** (CLAUDE.md updated) — فصحى, professional
   not stiff, no نقدم لكم / أفضل الأسعار / خدمة متميزة / يسعدنا أن.
7. Founding year = 2016. Hero triptych layout is INTENTIONAL — do not
   "fix" side-by-side slides.
8. Warranty buildings row = 10 years, own block, never a vehicle tier; no
   "lifetime" on buildings routes.

## HELD (approved to wait — never auto-resume)

- Merge to main + deploy: gated on smoke results + Ibrahim's explicit word.
- Building photography: placeholders stay, known gap (brief in
  ASSETS-NEEDED.md).
- bdm-flow real booking write path: post-launch.

## The law

CLAUDE.md as amended today (MSA rule). Lifetime wording only on
/warranty, /services/ppf, Premium Plus card, always with the qualifier.
No phone literals outside content/. Never `pnpm build` while a dev server
runs — they share `.next` and it corrupts.

## Exact next action

Ibrahim runs smoke checklist v2 (8 checks, in the 2026-08-07 conversation
transcript; owner mode, localhost:3000) and reports PASS/PARTIAL/FAIL per
check. Claude then fixes any FAILs via systematic-debugging, and on
Ibrahim's word merges `feat/building-heat-isolation` → main and deploys —
which also removes SK-BLD from the live site.
